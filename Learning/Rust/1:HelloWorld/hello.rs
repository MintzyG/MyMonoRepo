// Comments work just like in C
// Functions are declared using fn 
// The main function looks really similar to the C one
fn main() {
    // For some reason println has a '!' in it
    println!("Hello World!");
}
// We can compile this file using "rustc hello.rs"
